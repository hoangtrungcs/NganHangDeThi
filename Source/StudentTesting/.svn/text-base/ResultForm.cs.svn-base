using Telerik.WinControls.UI;

namespace StudentTesting {
    public partial class ResultForm : RadForm {
        public ResultForm() {
            InitializeComponent();
        }

        public void SetValue(string made, int usedTime, int rightNo, int wrongNo, int notyet) {
            testResult1.SetValue(made, usedTime, rightNo, wrongNo, notyet);
        }

        private void radButton1_Click(object sender, System.EventArgs e) {
            this.Close();
        }

    }
}
